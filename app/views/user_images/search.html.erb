<%= link_to "back to dashboard", dashboard_path(@user) %>
<% @images_found.each do |image| %>
  <ul>
    <li> <img src="<%= image.url %>" style="width: 200px;">
         <%= link_to "See more", show_image_url(image) %>
    </li>
  </ul>
<% end %>